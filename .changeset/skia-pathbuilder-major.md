---
"victory-native": major
---

Raise minimum `@shopify/react-native-skia` to `>=2.6.0` and migrate chart path construction to the immutable `PathBuilder` API. Raise minimum `react-native-reanimated` to `>=3.19.1` to align with Skia 2.6.
